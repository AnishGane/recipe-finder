import { LoginForm } from '@/forms/login-form'
import AuthLayout from '@/layout/auth-layout'

const Login = () => {
    return (
        <AuthLayout>
            <LoginForm />
        </AuthLayout>
    )
}

export default Login